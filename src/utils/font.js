import Typography from "typography"

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.75,
  bodyFontFamily: [
    "-apple-system",
    "Helvetica Neue",
    "Arial",
    "sans-serif",
  ],
})

export default typography
